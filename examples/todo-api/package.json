{
  "name": "@nexus/example-todo-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node dist/index.js",
    "build": "tsc",
    "dev": "node --watch src/index.ts",
    "test": "vitest run --no-cache"
  },
  "devDependencies": {
    "vitest": "^3.1.1",
    "typescript": "^5.8.2"
  }
}
